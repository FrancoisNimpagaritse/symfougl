{% extends 'admin/base.html.twig' %}

{% block title %}Cursus - Nouveau{% endblock %}

{% block body %}
    <!-- Begin Page Content -->
    <div class="container-fluid">
      <div class="text-center">
      <!-- Page Heading -->
      <h1 class="h3 mb-2 text-gray-800">Cursus académiques</h1>
      </div>
      <!-- DataTales Example -->
      <div class="card shadow mb-4">
        <div class="card-header py-3">
          <h6 class="m-0 font-weight-bold text-primary">Ajouter un cursus</h6>
        </div>
        <div class="card-body">
          <div class="col-md-6">
            {{form_start(form)}}
            {{form_widget(form)}}
            <button type="submit" class="btn btn-success">Enregister</button>
            {{form_end(form)}}
          </div>
        </div>
      </div>

    </div>
    <!-- /.container-fluid -->
{% endblock %}
